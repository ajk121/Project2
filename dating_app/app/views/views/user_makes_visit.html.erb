<h1>Listing views</h1>

<table>
  <tr>
    <th>People i visited</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @views.each do |view| %>
  <tr>
    <td><%= view.viewed_id %></td>
    <td><%= link_to 'Show', view %></td>
    <td><%= link_to 'Destroy', view, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

